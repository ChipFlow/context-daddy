name: Build Skills Package

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-skill:
    uses: robtaylor/skills/.github/workflows/skill-ci.yml@add-skill-ci
    with:
      skill_path: "."
      run_api_test: false
      run_claude_code_test: false
